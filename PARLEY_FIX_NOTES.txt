Parley production interaction fix - 2026-09-24

Changes:
- Post Server Ad is blue (primary), including protection against stale appearance overrides.
- Partnership CTAs are green, including Request Partnership and Partner Board actions.
- Find Partners now asks for a category first and includes an Any Category option.
- Slow partnership/search transitions acknowledge Discord before database work.
- Fixed the duplicate response/defer path in partnership request handling.
- Post Server Ad completes its deferred interaction instead of leaving a response hanging.
- Added per-user/per-action in-flight protection against rapid duplicate top-level clicks.
- Startup now refreshes existing live listing/partner-post views so old message buttons get the new styles.
- Each listing refresh fails independently so one deleted/forbidden Discord message cannot block startup.

Validation:
- Python compileall: passed.
- Main suite excluding subprocess startup-environment tests: 442 passed, 1 skipped.
- The 3 omitted startup subprocess checks cannot import Discord in this sandbox because those tests intentionally replace PYTHONPATH; this is a local test-environment limitation, not a project failure.

Security:
- .env, local database files, .git, and .venv are intentionally not included. Use Railway environment variables as before.

2026-09-25 NETWORK-FIRST UX PASS
--------------------------------
- New-server onboarding is Network-first: Setup Network (green), Post Server Ad (blue), How It Works.
- The server owner gets one onboarding DM per guild; an audit row prevents restart/reconnect spam.
- Network setup opened from the Parley hub now lists channels from the selected server, not the hub.
- Network setup defaults to a simple view: Partner Ad Channel + Enable/Save. Auto Partner filters/interval are under Advanced.
- Existing ads are one shared guild listing everywhere. Posting again explains that it already exists and opens Manage Existing Ad.
- Listing Manager now includes Setup Network so hub and local management use the same settings.
- Added /relist for server managers. It uses the same per-server persistent relist cooldown as the button.
- /find and /manage are hidden by default from ordinary guild members and re-check Manage Server live when used in a guild.
- Find Partners inside a connected server automatically represents that server instead of asking the manager to pick it again.
- Partnership/Network actions stay green; Post Server Ad stays blue.
- Accepted partnerships continue to exchange both ads only when both servers have enabled Network channels.

Validation in this environment:
- python -m compileall -q bot migrations: PASS
- pytest excluding 3 subprocess startup checks: 455 passed, 1 skipped
- The 3 excluded startup checks spawn a clean subprocess that cannot see discord.py in this sandbox; this is an environment limitation, not a project test regression.
