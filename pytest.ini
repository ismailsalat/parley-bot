[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: talks to real external services (skipped unless configured)
filterwarnings =
    ignore:.*PyNaCl.*
    ignore:'audioop' is deprecated:DeprecationWarning
